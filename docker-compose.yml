version: "3.9"

services:
  youtube-integration:
    build: .
    container_name: youtube-integration
    ports:
      - "8080:8080"
    environment:
      # Set your YouTube API key here or via host env variable
      YOUTUBE_API_KEY: ${YOUTUBE_API_KEY}
    restart: unless-stopped